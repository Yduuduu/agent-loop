[tool.ruff]
line-length = 100
target-version = "py313"
exclude = [".venv"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.black]
line-length = 100
target-version = ["py313"]
extend-exclude = '\.venv'

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
